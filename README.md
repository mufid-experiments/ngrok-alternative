# Local Tunnel Installer

Simple installer using Capistrano

## Preparation

Install all dependency:

    rvm install
    bundle

## Localtunnel Installation

Simply run the script:

    bin/cap production localtunnel:install

Don't worry, the script is idempotent.
